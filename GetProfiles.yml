info:
  name: GetProfiles
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/admin/profile/profile-details"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
