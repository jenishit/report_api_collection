info:
  name: Login
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/auth/login"
  headers:
    - name: auth
      value: beare asjkdfh
  body:
    type: json
    data: |-
      {
        "email": "jenish.admin@gmail.com",
        "password": "jenish@123"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
