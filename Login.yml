info:
  name: Login
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/auth/login
  headers:
    - name: auth
      value: beare asjkdfh
  body:
    type: json
    data: |-
      {
        "email": "jenish.brainztech@gmail.com",
        "password": "jenish@123"
      }
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
