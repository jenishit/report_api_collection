info:
  name: Create Role
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/role/create"
  body:
    type: json
    data: |-
      {
        "role_name": "ROLE_PATIENT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
