info:
  name: Create Role
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/role/create
  body:
    type: json
    data: |-
      {
        "role_name": "ROLE_TECHNICIAN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
