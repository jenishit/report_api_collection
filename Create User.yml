info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/user/create"
  body:
    type: json
    data: |-
      {
        "first_name": "Jenish",
        "last_name": "Doe",
        "email": "jenish.admin@gmail.com",
        "password": "jenish@123",
        "phone": "9866419050",
        "role_name": "ROLE_ADMIN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
