info:
  name: Insert Lab
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/admin/lab"
  headers:
    - name: auth
      value: beare asjkdfh
  body:
    type: json
    data: |-
      {
        "email": "jenish.brainztech@gmail.com",
        "password": "jenish@123"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
